# Observability Runbook

Logs
- Structured JSON with action, endpoint, status, latency, correlationId.

Metrics
- NHIE call success/fail, queue depth, retry counts, DLQ size.

Health
- /nhie/status endpoint and operator page checks.
