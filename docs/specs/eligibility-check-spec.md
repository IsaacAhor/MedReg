# Eligibility Check Spec

Flow
- Trigger at registration; query Coverage; cache 24h; manual refresh option.

UI States
- ACTIVE (green), EXPIRED (red), NOT FOUND (yellow), ERROR (grey).

Data
- NHIS number, coverage period, category; store with visit.
